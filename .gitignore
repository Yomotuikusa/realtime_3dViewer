# orch のローカル領域 (ルート直下のみ。別PCへは持っていかない)
/logs/
/.worktrees/
/.locks/
/.env
/.orch-migration/

# ビルド生成物・キャッシュ (どの階層でも)
target/
__pycache__/
*.pyc
node_modules/

# 依存の実体はリポジトリ外の /opt/3dreviewer/deps/ に置く (docs/…§23.2)
# .deps/ はリポジトリ内に置く二段構成を試した名残。使われていないが念のため残している
/.deps/
