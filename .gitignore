# orch のローカル領域 (ルート直下のみ。別PCへは持っていかない)
/logs/
/.worktrees/
/.locks/
/.env
/.orch-migration/

# ビルド生成物・キャッシュ (どの階層でも)
target/
__pycache__/
*.pyc
node_modules/

# 依存の実体 (workspace/node_modules のリンク先。config/sync-deps.sh で生成)
/.deps/

# sync-deps.sh が生成するマシン固有の symlink
/workspace/node_modules
